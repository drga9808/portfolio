---
interface Props {
  id: string;
  heading: string;
}

const { id, heading } = Astro.props;
---

<section id={id} class="py-20 px-6 max-w-5xl mx-auto">
  <h2 class="text-3xl font-bold mb-12 gradient-text inline-block">{heading}</h2>
  <slot />
</section>
